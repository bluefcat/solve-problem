#include <stdio.h>
#define MAX(X, Y) (((X) > (Y))? (X): (Y))

int array[1001];
int cache[1002][1002];

int main(){
	
	int n = 0;
	scanf("%d", &n);

	for(int i = 0; i < n; i ++) scanf("%d", array+i);
	
	for(int i = 0; i < n; i ++){
		for(int j = 0; j < i; j ++){
			array[j]
		
		}
	}

	return 0;
}
